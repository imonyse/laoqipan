<%= form_for current_user.relationships.build(:followed_id => object.id), :remote => true do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
	<div class="action"><%= f.submit t(".follow") %></div>
<% end %>